---
interface Props {
	date: Date;
}

const {date} = Astro.props;
---

<time class="pub-date" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-IN', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
