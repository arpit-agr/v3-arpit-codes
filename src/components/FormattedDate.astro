---
interface Props {
	date: Date;
}

const {date} = Astro.props;
---

<time class="text-step-00 tracking-wide" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-IN', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
