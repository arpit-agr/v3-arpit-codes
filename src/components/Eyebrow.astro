<div class="eyebrow grid">
	<p class="text-color-2 text-step-000 leading-flat tracking-wide uppercase font-medium">
		<slot />
	</p>
</div>

<style>
	.eyebrow {
		--gutter: var(--space-2xs);
		grid-template-columns: auto 1fr;
		align-items: center;

		&::after {
			content: '';
			inline-size: 100%;
			translate: 0 50%;
			border-block-start: var(--border-thickness-1) dashed var(--color-border-1);
		}
	}

	.eyebrow:has(+ .post-list-wrapper) {
		max-inline-size: var(--measure);
	}
</style>
