---
import type {HTMLAttributes} from 'astro/types';

interface Props extends HTMLAttributes<'a'> {
	href: string;
	label: string;
}

const {href, label} = Astro.props;
---

<a class="tracking-wide text-color-inverted-1" href={href}>
	{label}
</a>
